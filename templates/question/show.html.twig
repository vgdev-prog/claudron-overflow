{% extends 'base.html.twig' %}


{% block title %} Cloudfare | {{ question|capitalize }}{% endblock %}

{% block body %}

<h1>{{ question }}</h1>

    <div>
        Eventually, we'll print the full question here!
    </div>

    <h2>Answers ({{ answers|length }})</h2>
<ul>
    {% for answer in answers %}
    <li>{{ answer }}</li>
    {% endfor %}
</ul>

    <button class="btn-primary btn">1111</button>
{% endblock %}
